import styled from 'styled-components';
import BackGround from '../BackGround/Background';

const GameEndedField = styled(BackGround)`
  background-color: rgba(249, 219, 26, 0.3);
  align-items: center;
  align-content: center;
  display: flex;
  flex-direction: row;
  justify-content: center;
`;

export default GameEndedField;

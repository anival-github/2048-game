import styled from 'styled-components';

const GameEndedDescription = styled.div`
  font-size: 40px;
  color: white;
  text-transform: uppercase;
  font-weight: bold;
`;

export default GameEndedDescription;
